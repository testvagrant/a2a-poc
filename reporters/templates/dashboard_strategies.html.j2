<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Testing Strategies - UTA Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: rgba(255,255,255,0.95);
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            margin-bottom: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #1e3a8a;
            margin-bottom: 10px;
        }

        .header .subtitle {
            font-size: 1.2rem;
            color: #64748b;
        }

        .nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .nav-item {
            background: rgba(255,255,255,0.9);
            padding: 12px 24px;
            border-radius: 25px;
            text-decoration: none;
            color: #1e3a8a;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .nav-item:hover {
            background: #1e3a8a;
            color: white;
            transform: translateY(-2px);
        }

        .nav-item.active {
            background: #1e3a8a;
            color: white;
        }

        .section {
            background: rgba(255,255,255,0.95);
            border-radius: 16px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .section h2 {
            font-size: 2rem;
            color: #1e3a8a;
            margin-bottom: 20px;
        }

        .strategy-card {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 30px;
            transition: all 0.3s ease;
        }

        .strategy-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .strategy-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .strategy-title {
            flex: 1;
        }

        .strategy-title h3 {
            font-size: 1.5rem;
            color: #1e3a8a;
            margin-bottom: 5px;
        }

        .strategy-type {
            background: #e0f2fe;
            color: #0369a1;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .strategy-description {
            font-size: 1.1rem;
            color: #4b5563;
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .strategy-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .detail-item {
            background: white;
            padding: 15px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }

        .detail-label {
            font-size: 0.9rem;
            color: #64748b;
            margin-bottom: 5px;
            font-weight: 500;
        }

        .detail-value {
            font-size: 1rem;
            color: #1e293b;
            font-weight: 600;
        }

        .code-preview {
            background: #1e293b;
            color: #e2e8f0;
            padding: 20px;
            border-radius: 8px;
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 0.9rem;
            overflow-x: auto;
            margin-top: 20px;
        }

        .code-preview pre {
            margin: 0;
            white-space: pre-wrap;
        }

        .use-cases {
            background: #f0f9ff;
            border: 1px solid #bae6fd;
            border-radius: 8px;
            padding: 20px;
            margin-top: 20px;
        }

        .use-cases h4 {
            color: #0369a1;
            margin-bottom: 15px;
        }

        .use-cases ul {
            list-style: none;
            padding: 0;
        }

        .use-cases li {
            padding: 5px 0;
            color: #4b5563;
            position: relative;
            padding-left: 20px;
        }

        .use-cases li:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: #22c55e;
            font-weight: bold;
        }

        .intro-section {
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            color: white;
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            margin-bottom: 30px;
        }

        .intro-section h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .intro-section p {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 800px;
            margin: 0 auto;
        }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .nav {
                flex-direction: column;
                align-items: center;
            }
            
            .strategy-header {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🎯 Testing Strategies</h1>
            <div class="subtitle">Pluggable evaluation approaches for comprehensive AI agent testing</div>
        </div>

        <!-- Navigation -->
        <div class="nav">
            <a href="index.html" class="nav-item">📊 Overview</a>
            <a href="strategies.html" class="nav-item active">🎯 Strategies</a>
            <a href="scenarios.html" class="nav-item">📋 Scenarios</a>
            <a href="reports.html" class="nav-item">📈 Reports</a>
            <a href="architecture.html" class="nav-item">🏗️ Architecture</a>
        </div>

        <!-- Introduction -->
        <div class="intro-section">
            <h2>🧠 Intelligent Testing Strategies</h2>
            <p>
                Our pluggable strategy system allows you to test different aspects of AI agent behavior 
                with specialized evaluation approaches. Each strategy is designed to uncover specific 
                capabilities and potential issues.
            </p>
        </div>

        <!-- Strategy Details -->
        <div class="section">
            <h2>Available Strategies</h2>
            
            {% for strategy in strategies %}
            <div class="strategy-card">
                <div class="strategy-header">
                    <div class="strategy-title">
                        <h3>{{ strategy.name }}</h3>
                    </div>
                    <div class="strategy-type">Strategy</div>
                </div>
                
                <div class="strategy-description">
                    {{ strategy.description }}
                </div>
                
                <div class="strategy-details">
                    <div class="detail-item">
                        <div class="detail-label">Implementation</div>
                        <div class="detail-value">{{ strategy.file }}</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Type</div>
                        <div class="detail-value">Pluggable Strategy</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Status</div>
                        <div class="detail-value">✅ Active</div>
                    </div>
                </div>
                
                {% if strategy.code_preview %}
                <div class="code-preview">
                    <pre>{{ strategy.code_preview }}</pre>
                </div>
                {% endif %}
                
                <div class="use-cases">
                    <h4>Use Cases</h4>
                    <ul>
                        {% if "FlowIntent" in strategy.name %}
                        <li>Testing natural conversation flow and context understanding</li>
                        <li>Evaluating intent recognition and response appropriateness</li>
                        <li>Assessing multi-turn conversation coherence</li>
                        {% elif "ToolHappyPath" in strategy.name %}
                        <li>Testing successful function calling and tool usage</li>
                        <li>Evaluating structured data handling capabilities</li>
                        <li>Assessing API integration and response processing</li>
                        {% elif "MemoryCarry" in strategy.name %}
                        <li>Testing context retention across conversation turns</li>
                        <li>Evaluating memory and reference capabilities</li>
                        <li>Assessing long-term conversation coherence</li>
                        {% elif "ToolError" in strategy.name %}
                        <li>Testing error handling and recovery mechanisms</li>
                        <li>Evaluating graceful failure and user communication</li>
                        <li>Assessing robustness and reliability</li>
                        {% else %}
                        <li>Comprehensive AI agent evaluation</li>
                        <li>Quality assurance and testing</li>
                        <li>Performance benchmarking</li>
                        {% endif %}
                    </ul>
                </div>
            </div>
            {% endfor %}
        </div>

        <!-- Strategy Benefits -->
        <div class="section">
            <h2>🚀 Strategy Benefits</h2>
            <div class="strategy-details">
                <div class="detail-item">
                    <div class="detail-label">Modularity</div>
                    <div class="detail-value">Each strategy focuses on specific aspects of AI behavior</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Extensibility</div>
                    <div class="detail-value">Easy to add new strategies for custom evaluation needs</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Composability</div>
                    <div class="detail-value">Strategies can be combined for comprehensive testing</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Maintainability</div>
                    <div class="detail-value">Clean separation of concerns and easy updates</div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
