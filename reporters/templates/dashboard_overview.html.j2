<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UTA Dashboard - Universal Tester-Agent System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: rgba(255,255,255,0.95);
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            margin-bottom: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .header h1 {
            font-size: 3rem;
            font-weight: 700;
            color: #1e3a8a;
            margin-bottom: 10px;
        }

        .header .subtitle {
            font-size: 1.3rem;
            color: #64748b;
            margin-bottom: 20px;
        }

        .header .description {
            font-size: 1.1rem;
            color: #4b5563;
            max-width: 800px;
            margin: 0 auto;
        }

        .nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .nav-item {
            background: rgba(255,255,255,0.9);
            padding: 12px 24px;
            border-radius: 25px;
            text-decoration: none;
            color: #1e3a8a;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .nav-item:hover {
            background: #1e3a8a;
            color: white;
            transform: translateY(-2px);
        }

        .nav-item.active {
            background: #1e3a8a;
            color: white;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: rgba(255,255,255,0.95);
            border-radius: 16px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-value {
            font-size: 3rem;
            font-weight: 700;
            color: #1e3a8a;
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 1.1rem;
            color: #64748b;
            margin-bottom: 5px;
        }

        .stat-description {
            font-size: 0.9rem;
            color: #9ca3af;
        }

        .section {
            background: rgba(255,255,255,0.95);
            border-radius: 16px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .section h2 {
            font-size: 2rem;
            color: #1e3a8a;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .scenarios-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .scenario-category {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            padding: 20px;
        }

        .scenario-category h3 {
            color: #1e3a8a;
            margin-bottom: 15px;
            font-size: 1.3rem;
        }

        .scenario-list {
            list-style: none;
        }

        .scenario-item {
            padding: 8px 0;
            border-bottom: 1px solid #e2e8f0;
            color: #4b5563;
        }

        .scenario-item:last-child {
            border-bottom: none;
        }

        .scenario-item strong {
            color: #1e3a8a;
        }

        .strategies-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .strategy-card {
            background: #f0f9ff;
            border: 1px solid #bae6fd;
            border-radius: 12px;
            padding: 20px;
        }

        .strategy-card h3 {
            color: #0369a1;
            margin-bottom: 10px;
        }

        .strategy-card p {
            color: #4b5563;
            font-size: 0.9rem;
        }

        .reports-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .report-card {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            padding: 20px;
            transition: all 0.3s ease;
        }

        .report-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .report-card h3 {
            color: #1e3a8a;
            margin-bottom: 10px;
        }

        .report-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
            font-size: 0.9rem;
            color: #64748b;
        }

        .report-link {
            display: inline-block;
            background: #1e3a8a;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.9rem;
            transition: background 0.3s ease;
        }

        .report-link:hover {
            background: #1e40af;
        }

        .feature-highlight {
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            color: white;
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            margin-bottom: 30px;
        }

        .feature-highlight h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .feature-highlight p {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 800px;
            margin: 0 auto;
        }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .nav {
                flex-direction: column;
                align-items: center;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🤖 Universal Tester-Agent</h1>
            <div class="subtitle">AI Agent Testing & Evaluation Platform</div>
            <div class="description">
                A comprehensive, vendor-agnostic testing system for evaluating AI agents with sophisticated 
                multi-modal judging, real-time integration, and business-ready reporting.
            </div>
        </div>

        <!-- Navigation -->
        <div class="nav">
            <a href="index.html" class="nav-item active">📊 Overview</a>
            <a href="strategies.html" class="nav-item">🎯 Strategies</a>
            <a href="scenarios.html" class="nav-item">📋 Scenarios</a>
            <a href="reports.html" class="nav-item">📈 Reports</a>
            <a href="architecture.html" class="nav-item">🏗️ Architecture</a>
        </div>

        <!-- Feature Highlight -->
        <div class="feature-highlight">
            <h2>🚀 Agent-to-Agent Testing</h2>
            <p>
                Test AI agents with AI agents. Our system uses sophisticated LLM judges to evaluate 
                AI agent responses, providing nuanced insights that go beyond simple rule-based testing.
            </p>
        </div>

        <!-- Statistics -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-value">{{ stats.total_scenarios }}</div>
                <div class="stat-label">Test Scenarios</div>
                <div class="stat-description">Comprehensive test coverage across multiple domains</div>
            </div>
            <div class="stat-card">
                <div class="stat-value">{{ stats.total_strategies }}</div>
                <div class="stat-label">Testing Strategies</div>
                <div class="stat-description">Pluggable evaluation approaches</div>
            </div>
            <div class="stat-card">
                <div class="stat-value">{{ stats.total_reports }}</div>
                <div class="stat-label">Test Reports</div>
                <div class="stat-description">Business-ready evaluation results</div>
            </div>
            <div class="stat-card">
                <div class="stat-value">100%</div>
                <div class="stat-label">Real AI Testing</div>
                <div class="stat-description">Tests actual AI agents, not mocks</div>
            </div>
        </div>

        <!-- Scenarios Overview -->
        <div class="section">
            <h2>📋 Test Scenarios</h2>
            <div class="scenarios-grid">
                {% for category, scenarios in scenarios_by_category.items() %}
                <div class="scenario-category">
                    <h3>{{ category|title }} ({{ scenarios|length }})</h3>
                    <ul class="scenario-list">
                        {% for scenario in scenarios[:5] %}
                        <li class="scenario-item">
                            <strong>{{ scenario.id }}</strong><br>
                            {{ scenario.title }}
                        </li>
                        {% endfor %}
                        {% if scenarios|length > 5 %}
                        <li class="scenario-item">... and {{ scenarios|length - 5 }} more</li>
                        {% endif %}
                    </ul>
                </div>
                {% endfor %}
            </div>
        </div>

        <!-- Strategies Overview -->
        <div class="section">
            <h2>🎯 Testing Strategies</h2>
            <div class="strategies-grid">
                {% for strategy in strategies %}
                <div class="strategy-card">
                    <h3>{{ strategy.name }}</h3>
                    <p>{{ strategy.description }}</p>
                </div>
                {% endfor %}
            </div>
        </div>

        <!-- Recent Reports -->
        <div class="section">
            <h2>📈 Recent Test Reports</h2>
            <div class="reports-list">
                {% for report in recent_reports %}
                <div class="report-card">
                    <h3>{{ report.name|replace('out_', '')|title }}</h3>
                    <div class="report-meta">
                        <span>Model: {{ report.summary.model }}</span>
                        <span>Judge: {{ report.summary.judge_type|upper }}</span>
                    </div>
                    <div class="report-meta">
                        <span>{{ report.summary.pass_count }}/{{ report.summary.total }} passed</span>
                        <span>{{ report.summary.duration_s }}s</span>
                    </div>
                    <a href="{{ report.path }}" class="report-link">View Report</a>
                </div>
                {% endfor %}
            </div>
        </div>

        <!-- Key Features -->
        <div class="section">
            <h2>✨ Key Features</h2>
            <div class="strategies-grid">
                <div class="strategy-card">
                    <h3>🤖 Real AI Testing</h3>
                    <p>Test actual AI agents (ChatGPT, Claude, etc.) with real conversations, not mock responses.</p>
                </div>
                <div class="strategy-card">
                    <h3>🧠 LLM Judges</h3>
                    <p>Sophisticated AI-powered evaluation using GPT-4o for nuanced quality assessment.</p>
                </div>
                <div class="strategy-card">
                    <h3>📊 Business Reports</h3>
                    <p>Professional, stakeholder-ready reports with detailed insights and recommendations.</p>
                </div>
                <div class="strategy-card">
                    <h3>🔧 Pluggable Architecture</h3>
                    <p>Extensible system with pluggable strategies, judges, and adapters for any use case.</p>
                </div>
                <div class="strategy-card">
                    <h3>💰 Budget Control</h3>
                    <p>Cost monitoring and budget enforcement for production-ready testing.</p>
                </div>
                <div class="strategy-card">
                    <h3>🎯 Deterministic Testing</h3>
                    <p>Reproducible results with seeded RNG for consistent evaluation across runs.</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
