<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Architecture - UTA Dashboard</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; }
        .container { max-width: 1400px; margin: 0 auto; padding: 20px; }
        .header { background: rgba(255,255,255,0.95); border-radius: 16px; padding: 40px; text-align: center; margin-bottom: 30px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); }
        .header h1 { font-size: 2.5rem; font-weight: 700; color: #1e3a8a; margin-bottom: 10px; }
        .nav { display: flex; justify-content: center; gap: 20px; margin-bottom: 30px; flex-wrap: wrap; }
        .nav-item { background: rgba(255,255,255,0.9); padding: 12px 24px; border-radius: 25px; text-decoration: none; color: #1e3a8a; font-weight: 600; transition: all 0.3s ease; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .nav-item:hover { background: #1e3a8a; color: white; transform: translateY(-2px); }
        .nav-item.active { background: #1e3a8a; color: white; }
        .section { background: rgba(255,255,255,0.95); border-radius: 16px; padding: 30px; margin-bottom: 30px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
        .section h2 { font-size: 2rem; color: #1e3a8a; margin-bottom: 20px; }
        .components-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; }
        .component-card { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 12px; padding: 20px; }
        .component-card h3 { color: #1e3a8a; margin-bottom: 10px; }
        .component-card p { color: #4b5563; margin-bottom: 10px; }
        .component-location { font-family: monospace; background: #e2e8f0; padding: 4px 8px; border-radius: 4px; font-size: 0.9rem; }
        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 15px; }
        .feature-item { background: #f0f9ff; border: 1px solid #bae6fd; border-radius: 8px; padding: 15px; text-align: center; }
        .feature-item h4 { color: #0369a1; margin-bottom: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏗️ System Architecture</h1>
            <div class="subtitle">Scalable, modular design for comprehensive AI agent testing</div>
        </div>
        <div class="nav">
            <a href="index.html" class="nav-item">📊 Overview</a>
            <a href="strategies.html" class="nav-item">🎯 Strategies</a>
            <a href="scenarios.html" class="nav-item">📋 Scenarios</a>
            <a href="reports.html" class="nav-item">📈 Reports</a>
            <a href="architecture.html" class="nav-item active">🏗️ Architecture</a>
        </div>
        <div class="section">
            <h2>System Components</h2>
            <div class="components-grid">
                {% for component in architecture.components %}
                <div class="component-card">
                    <h3>{{ component.name }}</h3>
                    <p>{{ component.description }}</p>
                    <div class="component-location">{{ component.location }}</div>
                </div>
                {% endfor %}
            </div>
        </div>
        <div class="section">
            <h2>Key Features</h2>
            <div class="features-grid">
                {% for feature in architecture.features %}
                <div class="feature-item">
                    <h4>{{ feature }}</h4>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>
