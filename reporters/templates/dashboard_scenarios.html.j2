<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Scenarios - UTA Dashboard</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; }
        .container { max-width: 1400px; margin: 0 auto; padding: 20px; }
        .header { background: rgba(255,255,255,0.95); border-radius: 16px; padding: 40px; text-align: center; margin-bottom: 30px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); }
        .header h1 { font-size: 2.5rem; font-weight: 700; color: #1e3a8a; margin-bottom: 10px; }
        .nav { display: flex; justify-content: center; gap: 20px; margin-bottom: 30px; flex-wrap: wrap; }
        .nav-item { background: rgba(255,255,255,0.9); padding: 12px 24px; border-radius: 25px; text-decoration: none; color: #1e3a8a; font-weight: 600; transition: all 0.3s ease; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .nav-item:hover { background: #1e3a8a; color: white; transform: translateY(-2px); }
        .nav-item.active { background: #1e3a8a; color: white; }
        .section { background: rgba(255,255,255,0.95); border-radius: 16px; padding: 30px; margin-bottom: 30px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
        .section h2 { font-size: 2rem; color: #1e3a8a; margin-bottom: 20px; }
        .scenarios-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; }
        .scenario-category { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 12px; padding: 20px; }
        .scenario-category h3 { color: #1e3a8a; margin-bottom: 15px; font-size: 1.3rem; }
        .scenario-list { list-style: none; }
        .scenario-item { padding: 8px 0; border-bottom: 1px solid #e2e8f0; color: #4b5563; }
        .scenario-item:last-child { border-bottom: none; }
        .scenario-item strong { color: #1e3a8a; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📋 Test Scenarios</h1>
            <div class="subtitle">Comprehensive test coverage across multiple domains</div>
        </div>
        <div class="nav">
            <a href="index.html" class="nav-item">📊 Overview</a>
            <a href="strategies.html" class="nav-item">🎯 Strategies</a>
            <a href="scenarios.html" class="nav-item active">📋 Scenarios</a>
            <a href="reports.html" class="nav-item">📈 Reports</a>
            <a href="architecture.html" class="nav-item">🏗️ Architecture</a>
        </div>
        <div class="section">
            <h2>Available Test Scenarios</h2>
            <div class="scenarios-grid">
                {% for category, scenarios in scenarios.items() %}
                <div class="scenario-category">
                    <h3>{{ category|title }} ({{ scenarios|length }})</h3>
                    <ul class="scenario-list">
                        {% for scenario in scenarios %}
                        <li class="scenario-item">
                            <strong>{{ scenario.id }}</strong><br>
                            {{ scenario.title }}
                        </li>
                        {% endfor %}
                    </ul>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>
